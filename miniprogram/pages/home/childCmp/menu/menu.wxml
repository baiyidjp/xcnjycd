<!--pages/home/childCmp/menu/menu.wxml-->
<view class="menu">
  <view class="menu-header">{{ menuData.title }}</view>
  <block wx:for="{{ menuData.items }}" wx:key="id">
    <menu-item menuItem="{{ item }}" bindaddclick="addClick"></menu-item>
  </block>
</view>
